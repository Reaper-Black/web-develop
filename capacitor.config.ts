import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'integral-chat-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
